type: object
properties:
  template:
    type: string
    example: |
        "query": {
            "match": {
                "title": "{{query_string}}"
            }
        }
    description: Search template written in Mustache